import {db} from '../service/firebase.config';

const collFeedback = db.collection('evalueringer');


export const sendFeedbackToDatabase = async (user, data) => {
   /* const docRef =  await collFeedback.add({user});
   docRef.set({

   })  */   
   
   

  
}